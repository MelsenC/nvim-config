return {
  'Diogo-ss/five-server.nvim',
  dependencies = { 'nvim-lua/plenary.nvim' }, -- Optional, but recommended
  -- config = function()
    -- Configuration options here, if needed
    -- require('five-server').setup {
      -- Example:
      -- port = 3000,
      -- root = 'public',
    -- }
  -- end,
}
